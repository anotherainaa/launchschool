let array1 = [2, 6, 4];
let array2 = [2, 6, 4];

console.log(array1 === array2);

// This will output false. 
// When comparing two arrays, JS will check whether the two objects are the same object instead of comparing whether the values in the array are equal or not. 