// Write a loop that logs greeting three times.

let greeting = 'Aloha!';
let counter = 1

// while (counter <= 3) {
//   console.log(greeting);
//   counter += 1;
// }

// Here we use a while loop. 
// We must implement a counter to create a condition for this loop.

// We can also use for loop. 

for (let count = 1; count <= 3; count += 1) {
  console.log(greeting);
}