// Fix the code so that it stops after the first iteration.

for (let i = 0; ; i += 1) {
  console.log("and on");
  break;
}

// Assmuing that the question does not want us to add a conditional expression 
// we can use  `break` to break out of the loop after the first iteration 